import React from "react";

const API_LINK = import.meta.env.VITE_API_URL;

export default function MakeRoom() {
  return (
    <div>
      <div>
        <button>방 생성</button>
      </div>
    </div>
  );
}
